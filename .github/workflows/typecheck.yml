name: Type Check

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  typecheck:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: poker-mini-app
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
          cache-dependency-path: poker-mini-app/package-lock.json

      - name: Install dependencies
        run: npm ci

      # next-env.d.ts is gitignored but referenced by tsconfig.json; generate
      # it (plus route types) so CI typechecks the same ambient context as a
      # local checkout that has run next dev/build.
      - name: Generate Next.js types
        run: npx next typegen

      - name: Run TypeScript check
        run: npx tsc --noEmit --skipLibCheck
