name: Deploy poker-mini-app to Vercel

on:
  push:
    branches:
      - main
    paths:
      - "poker-mini-app/**"
      - ".github/workflows/deploy-poker-mini-app-vercel.yml"
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Deploy to Vercel
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          set -x
          cd poker-mini-app
          npx vercel --version
          npx vercel link --yes --scope wecrypto --project poker-mini-app --token "$VERCEL_TOKEN" 2>&1 || echo "LINK_EXIT=$?"
          npx vercel pull --yes --environment=production --token "$VERCEL_TOKEN" 2>&1 || echo "PULL_EXIT=$?"
          npx vercel build --prod --token "$VERCEL_TOKEN" 2>&1 || echo "BUILD_EXIT=$?"
          npx vercel deploy --prebuilt --prod --token "$VERCEL_TOKEN" 2>&1 || echo "DEPLOY_EXIT=$?"
