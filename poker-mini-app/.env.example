# Default values for local development
# For production, update these URLs to your deployed domain
KV_REST_API_TOKEN=''
KV_REST_API_URL=''
NEXT_PUBLIC_URL='http://localhost:3000'
NEXTAUTH_URL='http://localhost:3000'

# Shared multiplayer database (required for production/serverless hosting)
TURSO_DATABASE_URL=''
TURSO_AUTH_TOKEN=''

# FastAPI Backend URL
BACKEND_URL='http://localhost:8080'

# ASI:One API key — used as a fallback when the FastAPI backend is unreachable
ASI1_API_KEY=''
